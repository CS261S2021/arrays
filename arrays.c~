#include <stdio.h>


int main() {
  int size = 5;
  int favorite[5] = {2, 5, 7, 8, 11};
  int x = favorite[2];
  favorite[4] = x * 1000;

  int i;
  for(i=0; i<size; i++) {
    printf("favorite[%d] = %d\n", i,favorite[i]);
  }
}
